# Database Migrations

This directory holds migration scripts for the library management database.

## Usage

- Apply migrations before running the backend service.
- Run `001_init.sql` to create tables.
- Run `002_seed.sql` for demo data (optional).
- Add new migrations as the schema evolves.
